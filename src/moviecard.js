import React from 'react';

class MovieCard extends React.Component {
  render() {
    const { movieObject: { 
      title,
      subtitle,
      storyline,
      rating,
      imagePath

    } }
    return (
      <div>Movie Card</div>
    );
  }
}

export default MovieCard;
